# Task 3 — Обновления

## СТОП: задача заблокирована до ответа человека

`origin` этого репозитория — `https://github.com/jamber751/proxy-pilot.git`, **чужой репозиторий**. Обновление, тянущее exe из репозитория, которым человек не владеет, — это канал доставки чужого кода в процесс, который правит реестр и держит слушающий сокет.

**Не начинай, пока в этом файле не появится репозиторий-источник, названный человеком.** Если тебя всё же запустили — верни `BLOCKED` с этой причиной и не пиши код.

Источник обновлений: `________________` (заполняет человек)

## Обязательные свойства, когда задача разблокируется

- **Проверка не блокирует запуск** и не мешает работе: сеть может быть недоступна ровно тогда, когда прокси и нужен.
- **Скачанный файл проверяется подписью перед заменой.** При отсутствии подписи или неверной подписи обновление **не устанавливается** — не «устанавливается без проверки» и не «молча пропускается». Отключаемая проверка равна её отсутствию: отключённой она и окажется в тот единственный раз, когда важна.
- **Замена при следующем запуске, а не под работающим процессом.** Windows держит файл запущенного exe; подмена под собой процесса, который на выходе восстанавливает системный прокси, оставила бы реестр указывающим на мёртвый слушатель.
- **Пользователь может выключить проверку** обновлений (не проверку подписи).
- Дифференциальных обновлений не делаем — неоправданная сложность для инструмента такого размера.

## Приёмка

- [ ] Тесты сравнения версий: предрелизные, «текущая новее опубликованной», равные, мусор в теге.
- [ ] Тест поведения без сети — запуск не задерживается.
- [ ] Тест отказа при неверной подписи и при её отсутствии.
- [ ] Коммит: `feat(win): проверка и установка обновлений`
