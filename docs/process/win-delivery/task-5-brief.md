# Task 5 — Инструкция получателю

**Files:** `win/README.md`

## Что должно быть

- Что это и что делает.
- Куда смотреть после запуска (иконка в трее).
- **Что делать при предупреждении SmartScreen** — пока нет сертификата, оно будет у каждого получателя при первом запуске. Написать честно и прямо: почему оно появляется, что мы про это знаем, и что будет, когда сертификат появится. Умолчать значит получить вопрос в чате от каждого получателя по очереди.
- Как настроить системный прокси руками, если человек выключил автоматическое управление.
- Куда смотреть логи.

## Что НЕ покрывается — перечислить честно

Тот же список, что показывает диагностика: WinHTTP, Firefox (у него свои настройки прокси), приложения, читающие `HTTP_PROXY` из окружения. Сверься с текстом диагностики (`app/src/doctor.rs`) и не расходись с ним — два разных списка одного и того же хуже одного.

## Приёмка

- [ ] Список непокрытого совпадает с тем, что говорит диагностика.
- [ ] Ни одного обещания, которого продукт не выполняет.
- [ ] Коммит: `docs(win): инструкция для получателя`
