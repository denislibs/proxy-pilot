# SDD ledger — plan: docs/superpowers/plans/2026-08-30-proxypilot-win-vpn-static-ip.md

Spec: docs/superpowers/specs/2026-08-30-proxypilot-windows-rust-design.md (разделы 7 и 8)
Предыдущие планы: 1, 2 завершены; 3 в работе (задачи 1-5 закрыты)
Branch: feat/windows-rust
Toolchain: rustc 1.98.0

## Pre-flight scan

### Пары задач, делящие файл или интерфейс

| Пара | Что производит -> что потребляет | Результат |
|---|---|---|
| T1 -> T4 | `Installation` -> команды управления | Чисто по смыслу, **КОНФЛИКТ 1** по файлу: обе задачи владеют `openvpn.rs` |
| T2, T3, T5, T7 | все оперируют CIDR-подсетью | **КОНФЛИКТ 2** — тип `Ipv4Net` не имеет владельца ни в одной задаче |
| T2 | `default_routes(office_networks)` | **КОНФЛИКТ 3** — `OfficeNetwork` в core это GUID сети NLM, из него маршрут не выводится |
| T3 -> T4 | `foreign_tunnel_up` -> запрет подъёма | Чисто |
| T5 -> T6 | `decide_profile` -> служба | Чисто: T5 без ввода-вывода, T6 её вызывает |
| T6 | новый крейт `netsvc` | Чисто: единственная задача, правящая члены workspace |
| T7 -> плану 3 | меню и страница настроек | Чисто при условии «дописывает, не переписывает» |
| T4, T6 | живая машина | **КОНФЛИКТ 4** — ручная проверка обеих меняет сеть машины, на которой мы работаем |

### Внутренняя согласованность каждой задачи

| Задача | Результат |
|---|---|
| T1 | Чисто: интерфейс задан, «не установлен = Ok(None)» проверяемо |
| T2 | Конфликт 3 внутри собственного интерфейса; остальное (маска из битов, состав профиля) задано точно |
| T3 | Сигнатура `our_tunnel_up(...)` в плане не раскрыта |
| T4 | Чисто по коду; ручная проверка нереализуема агентом |
| T5 | Чисто: таблица решений полная, включая «чужая статика не сбрасывается» |
| T6 | Чисто по коду; установка и живой прогон нереализуемы агентом |
| T7 | Чисто, но объёмнее прочих: меню + страница + конфиг |

### Рулинги

Ruling: `Ipv4Net` живёт в `proxypilot-core` (новый модуль `net`), туда же уезжает `mask_of` — тип платформенно-нейтрален (четыре байта и длина префикса), а потребителей у него четыре в трёх крейтах; оставить его в `winnet::ovpn_profile` значило бы тянуть Windows-крейт в `core` ради структуры из пяти полей — Если неверно: одна лишняя правка импортов.

Ruling: T2 НЕ содержит `default_routes(office_networks)` — эта функция в плане несостоятельна: `OfficeNetwork` в конфиге хранит GUID сети NLM, а из GUID маршрут не выводится. Офисные подсети — отдельные данные, поле `office_subnets: Vec<Ipv4Net>`, и добавляет его T5 как владелец правок конфига в этом плане. T2 и T3 остаются чистыми функциями и конфиг не трогают — Если неверно: T2 пришлось бы вернуть, но уже с настоящим источником данных.

Ruling: T4 ДОПИСЫВАЕТ `openvpn.rs`, не переписывая `find_installation` из T1 — Если неверно: конфликт правок, виден компилятором.

Ruling: НИ ОДИН агент не выполняет на живой машине ни `netsh interface ipv4 set address`, ни `openvpn-gui.exe --command connect`, ни установку службы. Мы работаем внутри сессии на этой самой машине: снятый маршрут или потерянный адрес рвут связь, и откатить будет некому — а вся страховка (проверка шлюза, откат в DHCP) живёт в том самом коде, который в этот момент проверяется впервые. Ручные проверки T4 и T6 выносятся человеку и в отчёт как незакрытые — Если неверно: мы теряем раннее подтверждение и узнаём о промахе от человека, а не от теста; цена ошибки в другую сторону несопоставима.

Ruling: T6 не запускает `install-service` даже с правами — это единственный UAC во всём продукте, и он принадлежит человеку. Тесты службы идут на разборе конфигурации и формировании команд, без регистрации в SCM — Если неверно: ничего, регистрация проверяема человеком за одну минуту.

### Открытый вопрос к человеку (спека 8.6, план требует закрыть до T4)

Если снаружи доступен SSH-бастион, `ssh -D` даёт SOCKS5 внутрь офисной сети и вставляется как обычный апстрим уже написанным кодом — тогда задачи 1-4 не нужны вовсе. Это вопрос к администраторам. Пока ответа нет, делаем через OpenVPN: он у человека установлен и работает.

## Progress
Task 1: BASE=c2fae41
Task 1: implemented (commit 4d91ea5) — 289 тестов; OpenVPN найден на живой машине через реестр
Task 1: RegKey::open расширен параметром корня вместо второй обёртки — ревью проверило все 10 точек вызова поимённо: каждая прежняя по-прежнему HKCU, единственный HKLM открыт KEY_READ и ни одному писателю не передаётся, порядок записи в sysproxy и его тесты не тронуты
Task 1: fix round 1/5 (5 findings dispatched) — Important: CLAUDE.md устарел в тот же день, когда написан (ошибка контроллера, а не реализатора)
Task 1: Ruling: 32-битное представление реестра (WOW6432Node) НЕ поддерживаем, только документируем — отказ безопасный (ложное «не установлен» гасит раздел с внятным объяснением), а OpenVPN 2.6+ существует только 64-битным, то есть retry с KEY_WOW64_32KEY писался бы под конфигурацию, о которой у нас нет ни одного свидетельства. Неиспользуемый код это обязательство, а не страховка — Если неверно: придёт реальный отчёт, и retry добавляется одной правкой поверх комментария
Task 1: fix round 1/5 (5 addressed; commits 4d91ea5..adf41ce) — 290 тестов; тест на транспозицию BIN_DIR/CONFIG_DIR пишет в одноразовый ключ HKCU с суффиксом PID и Drop-уборкой
Task 1: complete (commits c2fae41..adf41ce) — 290 тестов, клиппи и формат чисто, HKCU\Software после прогона чист
Task 1: поправка ко мне — CLAUDE.md разошёлся с кодом в день написания: строка «реестр: только HKCU» устарела этой же задачей. Исправлено; сам файл называет такое расхождение дефектом
Task 2: BASE=41cf379
Task 2: implemented (commit 91b235d) — 309 тестов; идемпотентность через блок с маркерами, заявлена реализатором на разбор
Task 2: ревью признало подход с маркерами верным: вычистка block-outside-dns идёт по строкам ВНЕ блока, поэтому пересборка поверх собранного — неподвижная точка, а восстановленная человеком директива вычищается снова
Task 2: fix round 1/5 (9 findings dispatched) — Important: непарный маркер BEGIN обрезает профиль вместе с сертификатами; хостовые биты в маршруте не маскируются (203.0.113.5/24 -> route 203.0.113.5), то есть ровно тот класс отказа, ради которого задача и писалась
Task 2: Ruling: маскировать хостовые биты В ДВУХ местах — нормализация в Ipv4Net::from_str (одно представление, Display круглится, страница настроек показывает человеку, что мы поняли) И защитная маскировка при выводе маршрута (поля структуры публичные, конструирование мимо from_str возможно, а задачи 3, 5, 7 будут эти значения строить). Не дублирование, а два разных входа — Если неверно: следующий читатель удалит одну из них как лишнюю, поэтому причина записана в коде
Task 2: ревью методологически сильное: собран отдельный пробник вне репозитория, подключающий настоящий модуль через #[path], арифметика маски и патологические случаи проверены независимо от тестов задачи
