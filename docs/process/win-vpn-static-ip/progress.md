# SDD ledger — plan: docs/superpowers/plans/2026-08-30-proxypilot-win-vpn-static-ip.md

Spec: docs/superpowers/specs/2026-08-30-proxypilot-windows-rust-design.md (разделы 7 и 8)
Предыдущие планы: 1, 2 завершены; 3 в работе (задачи 1-5 закрыты)
Branch: feat/windows-rust
Toolchain: rustc 1.98.0

## Pre-flight scan

### Пары задач, делящие файл или интерфейс

| Пара | Что производит -> что потребляет | Результат |
|---|---|---|
| T1 -> T4 | `Installation` -> команды управления | Чисто по смыслу, **КОНФЛИКТ 1** по файлу: обе задачи владеют `openvpn.rs` |
| T2, T3, T5, T7 | все оперируют CIDR-подсетью | **КОНФЛИКТ 2** — тип `Ipv4Net` не имеет владельца ни в одной задаче |
| T2 | `default_routes(office_networks)` | **КОНФЛИКТ 3** — `OfficeNetwork` в core это GUID сети NLM, из него маршрут не выводится |
| T3 -> T4 | `foreign_tunnel_up` -> запрет подъёма | Чисто |
| T5 -> T6 | `decide_profile` -> служба | Чисто: T5 без ввода-вывода, T6 её вызывает |
| T6 | новый крейт `netsvc` | Чисто: единственная задача, правящая члены workspace |
| T7 -> плану 3 | меню и страница настроек | Чисто при условии «дописывает, не переписывает» |
| T4, T6 | живая машина | **КОНФЛИКТ 4** — ручная проверка обеих меняет сеть машины, на которой мы работаем |

### Внутренняя согласованность каждой задачи

| Задача | Результат |
|---|---|
| T1 | Чисто: интерфейс задан, «не установлен = Ok(None)» проверяемо |
| T2 | Конфликт 3 внутри собственного интерфейса; остальное (маска из битов, состав профиля) задано точно |
| T3 | Сигнатура `our_tunnel_up(...)` в плане не раскрыта |
| T4 | Чисто по коду; ручная проверка нереализуема агентом |
| T5 | Чисто: таблица решений полная, включая «чужая статика не сбрасывается» |
| T6 | Чисто по коду; установка и живой прогон нереализуемы агентом |
| T7 | Чисто, но объёмнее прочих: меню + страница + конфиг |

### Рулинги

Ruling: `Ipv4Net` живёт в `proxypilot-core` (новый модуль `net`), туда же уезжает `mask_of` — тип платформенно-нейтрален (четыре байта и длина префикса), а потребителей у него четыре в трёх крейтах; оставить его в `winnet::ovpn_profile` значило бы тянуть Windows-крейт в `core` ради структуры из пяти полей — Если неверно: одна лишняя правка импортов.

Ruling: T2 НЕ содержит `default_routes(office_networks)` — эта функция в плане несостоятельна: `OfficeNetwork` в конфиге хранит GUID сети NLM, а из GUID маршрут не выводится. Офисные подсети — отдельные данные, поле `office_subnets: Vec<Ipv4Net>`, и добавляет его T5 как владелец правок конфига в этом плане. T2 и T3 остаются чистыми функциями и конфиг не трогают — Если неверно: T2 пришлось бы вернуть, но уже с настоящим источником данных.

Ruling: T4 ДОПИСЫВАЕТ `openvpn.rs`, не переписывая `find_installation` из T1 — Если неверно: конфликт правок, виден компилятором.

Ruling: НИ ОДИН агент не выполняет на живой машине ни `netsh interface ipv4 set address`, ни `openvpn-gui.exe --command connect`, ни установку службы. Мы работаем внутри сессии на этой самой машине: снятый маршрут или потерянный адрес рвут связь, и откатить будет некому — а вся страховка (проверка шлюза, откат в DHCP) живёт в том самом коде, который в этот момент проверяется впервые. Ручные проверки T4 и T6 выносятся человеку и в отчёт как незакрытые — Если неверно: мы теряем раннее подтверждение и узнаём о промахе от человека, а не от теста; цена ошибки в другую сторону несопоставима.

Ruling: T6 не запускает `install-service` даже с правами — это единственный UAC во всём продукте, и он принадлежит человеку. Тесты службы идут на разборе конфигурации и формировании команд, без регистрации в SCM — Если неверно: ничего, регистрация проверяема человеком за одну минуту.

### Открытый вопрос к человеку (спека 8.6, план требует закрыть до T4)

Если снаружи доступен SSH-бастион, `ssh -D` даёт SOCKS5 внутрь офисной сети и вставляется как обычный апстрим уже написанным кодом — тогда задачи 1-4 не нужны вовсе. Это вопрос к администраторам. Пока ответа нет, делаем через OpenVPN: он у человека установлен и работает.

## Progress
Task 1: BASE=c2fae41
Task 1: implemented (commit 4d91ea5) — 289 тестов; OpenVPN найден на живой машине через реестр
Task 1: RegKey::open расширен параметром корня вместо второй обёртки — ревью проверило все 10 точек вызова поимённо: каждая прежняя по-прежнему HKCU, единственный HKLM открыт KEY_READ и ни одному писателю не передаётся, порядок записи в sysproxy и его тесты не тронуты
Task 1: fix round 1/5 (5 findings dispatched) — Important: CLAUDE.md устарел в тот же день, когда написан (ошибка контроллера, а не реализатора)
Task 1: Ruling: 32-битное представление реестра (WOW6432Node) НЕ поддерживаем, только документируем — отказ безопасный (ложное «не установлен» гасит раздел с внятным объяснением), а OpenVPN 2.6+ существует только 64-битным, то есть retry с KEY_WOW64_32KEY писался бы под конфигурацию, о которой у нас нет ни одного свидетельства. Неиспользуемый код это обязательство, а не страховка — Если неверно: придёт реальный отчёт, и retry добавляется одной правкой поверх комментария
Task 1: fix round 1/5 (5 addressed; commits 4d91ea5..adf41ce) — 290 тестов; тест на транспозицию BIN_DIR/CONFIG_DIR пишет в одноразовый ключ HKCU с суффиксом PID и Drop-уборкой
Task 1: complete (commits c2fae41..adf41ce) — 290 тестов, клиппи и формат чисто, HKCU\Software после прогона чист
Task 1: поправка ко мне — CLAUDE.md разошёлся с кодом в день написания: строка «реестр: только HKCU» устарела этой же задачей. Исправлено; сам файл называет такое расхождение дефектом
Task 2: BASE=41cf379
Task 2: implemented (commit 91b235d) — 309 тестов; идемпотентность через блок с маркерами, заявлена реализатором на разбор
Task 2: ревью признало подход с маркерами верным: вычистка block-outside-dns идёт по строкам ВНЕ блока, поэтому пересборка поверх собранного — неподвижная точка, а восстановленная человеком директива вычищается снова
Task 2: fix round 1/5 (9 findings dispatched) — Important: непарный маркер BEGIN обрезает профиль вместе с сертификатами; хостовые биты в маршруте не маскируются (203.0.113.5/24 -> route 203.0.113.5), то есть ровно тот класс отказа, ради которого задача и писалась
Task 2: Ruling: маскировать хостовые биты В ДВУХ местах — нормализация в Ipv4Net::from_str (одно представление, Display круглится, страница настроек показывает человеку, что мы поняли) И защитная маскировка при выводе маршрута (поля структуры публичные, конструирование мимо from_str возможно, а задачи 3, 5, 7 будут эти значения строить). Не дублирование, а два разных входа — Если неверно: следующий читатель удалит одну из них как лишнюю, поэтому причина записана в коде
Task 2: ревью методологически сильное: собран отдельный пробник вне репозитория, подключающий настоящий модуль через #[path], арифметика маски и патологические случаи проверены независимо от тестов задачи
Task 2: fix round 1/5 (9 addressed; commits 91b235d..2a444a5) — 324 теста
Task 2: ЗАЯВЛЕНО РЕАЛИЗАТОРОМ САМОСТОЯТЕЛЬНО: он начал сочинять «восстановленный» лог красного прогона для находки 1, выбросил его и вместо этого вернул исходный код 91b235d под новые тесты, получив настоящие отказы. Раскрыл сам, хотя мог не раскрывать — это и есть то, ради чего требование дословного свидетельства существует. Ре-ревью велено проверить согласованность, а не зачесть на доверии
Task 2: ре-ревью круга 1 — NEEDS REWORK. Critical: обрезание профиля не устранено, а отложено на одну пересборку — непарный BEGIN из источника выживает в результате, наш добавленный блок даёт ему END, и следующая сборка удаляет всё между ними вместе с сертификатом. Тест проверял только первый проход
Task 2: Ruling: для каждой правки в обработке маркеров проверять ВТОРУЮ сборку, а не только первую — ошибки идемпотентности прячутся ровно на один вызов вглубь, и первый проход выглядит верным. Задача 5 будет пересобирать профиль при каждом изменении подсетей, то есть вторая сборка это обычный ход событий — Если неверно: лишний assert в каждом тесте маркеров
Task 2: ре-ревью подтвердило двойную маскировку живой и покрытой с ОБЕИХ сторон (тест конструирует Ipv4Net через публичные поля мимо from_str) — это было самое вероятное место тихого возврата к одинарной
Task 2: ре-ревью независимо восстановило красное свидетельство (старый код под новые тесты) и подтвердило подлинность по номерам строк и значениям в панике
Task 2: fix round 2/5 (1 Critical + 5 dispatched)
Task 2: поправка ко мне (вторая за план): правило про данные в CLAUDE.md написано слишком широко — буквально оно запрещает 192.168.0.0/16 и 10.0.0.0/8 в DEFAULT_NO_PROXY, а это рабочее поведение продукта. Читатель, исполнивший букву, сломал бы список обхода. Смысл правила — не пускать в публичный репозиторий НАСТОЯЩИЕ адреса рабочей сети; общие частные диапазоны как иллюстрация не раскрывают ничего
Task 2: fix round 2/5 (9 addressed; commits 2a444a5..f448a9c) — 328 тестов; машина маркеров переписана на одиночный проход
Task 2: ре-ревью круга 2 — NEEDS REWORK, три Important. Машина маркеров признана исправной по всему пространству входов (три сборки, включая CRLF и повторную подачу собранного профиля), но сломалась НОВАЯ зависимость — отслеживание встроенных блоков
Task 2: ЗАКОНОМЕРНОСТЬ, названная на третьем круге: каждая правка вводит новый механизм, и его краевые случаи становятся следующим дефектом. Круг 1 чинил случай, круг 2 заменил парный поиск маркеров, круг 3 добавил отслеживание встроенных блоков — и каждый раз ломалось новое
Task 2: Ruling: структурная причина — в файле ТРИ независимых классификатора строк, и они расходятся (lines_to_drop учитывает встроенные блоки, redirect_gateway_present нет, предикаты открытия и закрытия тега спорят об аннотированных тегах). Правка: один проход классифицирует строку один раз, все прочие функции ПОТРЕБЛЯЮТ классификацию, а не выводят её заново. Один классификатор, покрытый тестами, и есть то, что не даёт появиться четвёртому мнению — Если неверно: четвёртый круг на том же месте
Task 2: Ruling: build_profile переводится на Result и ОТКАЗЫВАЕТСЯ строить из структурно битого источника (непарный встроенный тег), а не угадывает. Незакрытый <ca> и так невалиден для OpenVPN, но OpenVPN падает громко, а мы молча портим. Момент самый дешёвый: задача 4 будет первым вызывающим, задачи 3, 5, 7 не написаны — Если неверно: сигнатура шире нужного, стоимость одна правка
Task 2: худшая по последствиям находка (C): pull-filter внутри <connection> заставлял пропустить фильтр верхнего уровня, то есть ВЕСЬ трафик шёл бы через офис постоянно — ровно тот отказ, ради предотвращения которого строка и существует, и устойчивая неподвижная точка, которая сама не исправится
Task 2: fix round 3/5 (перестройка; commits f448a9c..79ae61f) — 348 тестов; три несогласных классификатора строк заменены одним проходом classify_lines, build_profile переведён на Result и отказывается строить из битого источника
Task 2: ре-ревью круга 3 — APPROVED, класс дефектов закрыт. Самая сильная проверка за проект: 59 ручных случаев по три сборки, затем дифференциальный фаззинг 300 000 случайных источников против независимой эталонной реализации, написанной по брифу, четыре сборки с чередующимися наборами маршрутов. Ноль расхождений с эталоном, ноль нарушений неподвижной точки, фильтр DNS не появился ни разу
Task 2: три Minor закрыты контроллером напрямую (правки на несколько строк, отдельного цикла не стоили): debug_assert в недостижимой ветке маркеров, объяснение несимметрии отказа для непарных тегов, объяснение сужения вычистки block-outside-dns до верхнего уровня
Task 2: Ruling: недостижимая ветка Begin/End получает debug_assert!(false) вместо молчания — тихое удаление строки с маркером есть ровно тот отказ, который в этой задаче возвращался ТРИЖДЫ; в релизе молчим, чтобы не ронять сборку из-за своей же ошибки, в тестах падаем громко — Если неверно: ничего, ветка недостижима по построению и подтверждена фаззингом
Task 2: complete (commits 41cf379..HEAD) — 348 тестов, 3 круга правок
Task 3: BASE=31ef54c
Task 3: implemented — 359 тестов (было 348+3 ignored, прирост 11).
`crates/winnet/src/tunnel_state.rs`: `AdapterRoute` как в брифе,
`our_tunnel_up`/`foreign_tunnel_up` чистыми функциями поверх
`proxypilot_core::net::Ipv4Net`. Пересечение считается по границам
диапазона (`mask_of` из core), правило для широкого/узкого маршрута —
пересечение в любую сторону, третьего варианта у выровненных CIDR-блоков
не бывает
Task 3: отклонение от сигнатуры плана — `foreign_tunnel_up` получила
третий параметр `our_alias: &str`, которого в брифе не было. Без него
функция не могла бы сама отличить наш уже поднятый туннель от чужого, а
«наш туннель не считается чужим» — прямое требование приёмки,
тестируемое как свойство этой функции, а не поведение вызывающего кода.
Функция осталась чистой — вызывающий (задачи 4, 7) и так знает
`our_alias`, тот же аргумент уже идёт в `our_tunnel_up`
Task 3: живая таблица маршрутов проверена только на чтение
(`route print -4`) — адаптеры Wintun/TAP/OpenVPN Data Channel Offload
присутствуют в системе, но ни один не несёт активного маршрута сейчас;
фикстуры для Tailscale и чужого туннеля собраны вручную, как и
предполагал бриф
Task 3: complete (commits 31ef54c..HEAD) — 359 тестов, клиппи и формат чисто
Task 3: fix round 1/5 (2 Important + 4 Minor dispatched) — ревьюер
прогнал `overlaps` против независимой интервальной арифметики на 200 704
парах CIDR, ноль расхождений; отклонение сигнатуры (`our_alias`) принято
без изменений
Task 3: Important — байт-в-байт сравнение алиаса давало дедлок: чужое
написание регистра или пробел на нашем же адаптере одновременно гасили
`our_tunnel_up` и включали `foreign_tunnel_up`, то есть блокировали и
подъём, и останов нашего собственного туннеля. Исправлено через
`same_alias` (trim + `to_lowercase`); отдельно задокументировано более
глубокое и неисправленное этим ограничение — алиас интерфейса Windows не
устойчивый идентификатор (переименовываем пользователем, по-разному
отображается разными утилитами) — задачи 4 и 7 должны знать об этом, а
не удивляться на живой машине
Task 3: Important — отчёт задачи называл реальные подсети этой машины
(Wi-Fi и локальный виртуальный адаптер), полученные из `route print`.
Исправлено до сборки коммита — утёкший текст не вошёл в публичную
историю. Правило CLAUDE.md про данные распространяется на `docs/process/`
по имени именно из-за прецедента этой же формы
Task 3: fix round 1/5 (6 addressed; commit 5a6c4d3..HEAD, будет squash
контроллером) — 363 теста; 4 новых теста (`0.0.0.0/0`, `/32` внутри `/16`,
хостовые биты мимо `FromStr`, регистронезависимость алиаса),
`#[derive(Debug)]` на `AdapterRoute`, повторный импорт `Ipv4Net` в тестах
убран
Task 4: BASE=3bc64f2
Task 4: implemented — `crates/winnet/src/openvpn.rs` дописан (не
переписан): `install_profile`, `connect`, `disconnect`, `status` из
брифа плюс пятая функция сверх списка — `build_and_install_profile`, без
которой требование «ты первый вызывающий `build_profile`, ошибка не
проглатывается» было бы невыполнимо буквальной сигнатурой `install_profile
(inst, name, contents: &str)`, принимающей уже готовый текст. 377 тестов
(было 363, +14); клиппи и формат чисто
Task 4: TunnelStatus различает только «файл профиля на диске есть/нет»,
не «поднят/опущен» — осознанно, не пробел: у `openvpn-gui.exe` нет
синхронного текстового запроса состояния (только `connect`/`disconnect`,
`docs/design.md` §8.3), а единственный источник живого состояния —
`tunnel_state::our_tunnel_up` (задача 3), чей alias интерфейса Windows не
устойчивый идентификатор. Смешивать «файл есть» с «значит, поднято» было
бы нечестно; докблок `TunnelStatus` говорит об этом прямо
Task 4: все четыре функции управления (`install_profile`, `connect`,
`disconnect`, `status`) заново проверяют `gui_exe.is_file()`
(`ensure_still_installed`) при каждом вызове, а не доверяют однажды
найденному `Installation` — OpenVPN мог быть удалён между поиском
(задача 1) и вызовом. `WinNetError::OpenVpnNotFound` — общий отказ на все
четыре
Task 4: командная строка (`build_gui_command`) — чистая функция,
собирает `std::process::Command`, ничего не запускает; `connect`/
`disconnect` вызывают `.spawn()` (не `.status()`), потому что при ещё не
запущенном GUI сам процесс становится долгоживущим окном в трее —
`.status()` заблокировался бы на неопределённое время. `Command::spawn`
— ровно одна точка вызова во всём модуле, тестами не достигается: перед
ней везде стоит `ensure_still_installed`
Task 4: живой прогон не выполнялся ни разу — ни `openvpn-gui.exe
--command connect|disconnect`, ни `--help`/version-проба (не
понадобилась: до `Command::spawn` тесты не доходят), ни запись в
настоящий каталог `config` установленного на машине OpenVPN. Ручная
проверка (поднять туннель, внутренний ресурс доступен, внешний IP
остался домашним, опустить) вынесена человеку
Task 4: complete (commits 3bc64f2..HEAD) — 377 тестов, 3 ignored, клиппи
и формат чисто
Task 4: approved with fixes (7 findings dispatched, 0 Critical) — оба
заявленных отступления от брифа приняты по существу (build_and_install_
profile — брифная пара инструкций была невыполнима вместе, не ошибка
реализатора; TunnelStatus — разделение труда с tunnel_state верное, имя
нет)
Task 4: Important — `TunnelStatus`/`status` переименованы в
`ProfileStatus`/`profile_status`: тип и функция отвечали на вопрос «есть
ли файл профиля на диске», но назывались как будто отвечают на «поднят
ли туннель» — `status(...) == Installed` читался бы как «подключено» тем,
кто не открыл докблок
Task 4: Important — докблоки `connect`/`disconnect` теперь прямо говорят,
что `Ok` значит «команда доставлена GUI», не «туннель поднят»: раньше это
было видно только в докблоке приватной `run_gui_command` и в
`WinNetError::OpenVpnGuiLaunch`, а не в контракте публичной функции
Task 4: Important — тестовый хелпер `cleanup` делал `remove_dir_all` без
проверки, что путь временный; безопасно только по соглашению вызывающих
(все текущие вызовы передают фикстуру), один copy-paste на настоящий
`Installation` снёс бы `bin` установленного OpenVPN. Добавлена проверка
`starts_with(std::env::temp_dir())`
Task 4: fix round 1/5 (7 addressed; commit поверх e968d52) — 379 тестов
(было 377, +2: тест на путь с пробелом в `build_gui_command`, тест на
перезапись существующего профиля); клиппи и формат чисто. Седьмая
находка (кому передавать наш `our_alias` для `tunnel_state`) — не код
этой задачи, зафиксирована для брифа задачи 7
Task 3: implemented (commit 5a6c4d3) — 359 тестов; сигнатура foreign_tunnel_up расширена our_alias, отступление заявлено реализатором и признано верным: без него «свой туннель не чужой» перестаёт быть свойством функции и становится обязанностью задач 4 и 7, ни одна из которых на это не проверяется
Task 3: ревью подтвердило правило перекрытия перебором 200 704 пар CIDR против независимой интервальной арифметики — ноль расхождений. Реализация оказалась ЛУЧШЕ собственного обоснования: общий интервальный тест верен и для невыровненных значений, которые допускают публичные поля Ipv4Net
Task 3: fix round 1/5 (6 addressed; commits 5a6c4d3..d59432d) — 363 теста
Task 3: Important 1 — побайтовое сравнение имён адаптеров загоняло приложение в тупик: расхождение в регистре делало НАШ поднятый туннель чужим, а при чужом туннеле правило запрещает и подъём, и останов, то есть опустить свой же туннель стало бы нечем
Task 3: унаследованное ограничение (заявлено, не скрыто): имя интерфейса в Windows — не устойчивая личность, оно переименуемо и по-разному форматируется разными API. Переименование при поднятом туннеле по-прежнему сделает его чужим. Устойчивы LUID или индекс интерфейса. Записано в докблок модуля, чтобы задачи 4 и 7 унаследовали знание, а не сюрприз
Task 3: УТЕЧКА, третий случай одной формы: отчёт записал реальные подсети машины; при исправлении реализатор процитировал их заново в описании исправления. Убрано контроллером вручную. Правило про данные нарушается не самими данными, а РАССКАЗОМ о работе с ними — это уже третий раз, и формулировку в CLAUDE.md стоит усилить именно на этом
Task 3: коммиты задачи схлопнуты в один до заливки — утечка не попала в публичную историю вовсе, а не исправлена коммитом поверх
Task 4: implemented (commit e968d52) — 377 тестов; два отступления заявлены реализатором, оба признаны верными
Task 4: Ruling: пятая функция build_and_install_profile принята — две инструкции моего брифа (четыре функции И «быть первым вызывающим build_profile, не проглатывая ошибку») несовместимы при сигнатуре install_profile, берущей готовый текст. Ошибка брифа моя; решение в две строки, а не новый шов. Проброс настоящий: ? над #[from] ProfileError, тест кормит незакрытый <ca> и проверяет И тип ошибки, И что файл не появился
Task 4: Ruling: ProfileStatus вместо TunnelStatus — разделение труда верное (задача 3 отвечает по маршрутам «поднят ли», эта — «установлен ли профиль») и задокументировано там, где смотрит автор меню, но ИМЯ противоречило документации. Имя читают первым, документацию пропускают; Ok(TunnelStatus::Installed) из функции status кто-нибудь привязал бы к надписи «Подключено» — Если неверно: ничего, имя точнее
Task 4: тихая мина, найденная ревью: тестовый cleanup рекурсивно удалял каталог с openvpn-gui.exe. Все вызовы подсовывали заглушку, но одно копирование в тест с настоящей установкой снесло бы bin OpenVPN с машины разработчика. Разрушительный помощник, безопасный только по договорённости
Task 4: fix round 1/5 (7 addressed; commits e968d52..18faca2) — 379 тестов
Task 4: ревью доказало безопасность ЧТЕНИЕМ до запуска набора: .spawn() в крейте ровно один, достижим только после ensure_still_installed, тесты подсовывают заведомо несуществующий путь. Это правильный порядок, когда OpenVPN на машине реально установлен
Task 4: правило о данных соблюдено полностью (первый раз за план) — в диффе только RFC 5737, отчёт описывает ручные проверки по смыслу, не называя ни хостов, ни подсетей, ни адаптеров
Task 4: ДЛЯ ЗАДАЧИ 7 (моё, не реализатора): ничто пока не задаёт НАШ алиас интерфейса, поэтому our_tunnel_up нельзя вызвать с верным аргументом до задачи 7
Task 4: ЖИВАЯ ПРОВЕРКА ЗА ЧЕЛОВЕКОМ: поднять туннель из приложения; убедиться, что внутренний ресурс доступен; сверить внешний IP до и после — он обязан остаться домашним (split-tunnel, не полный); опустить и убедиться, что прочий трафик не прерывался
Task 4: complete (commits 3bc64f2..18faca2) — 379 тестов
Task 5: implemented (commit 501f928) — 391 тест; заявлены две правки: ручной serde для Ipv4Net через Display/FromStr и упаковка Change::Whole в Box (клиппи на large_enum_variant после роста Config)
Task 5: ревью проверило инвариант порта поимённо после упаковки — #[must_use] на том же элементе с тем же текстом, правило по-прежнему хвостовое выражение apply_change, разделение saved/live цело, три закрепляющих теста отличаются ТОЛЬКО обёрткой Box::new. Упаковка убрала разыменование, то есть перемещений стало меньше. Прецедент Cmd::ApplyConfig настоящий
Task 5: Ruling: поле addr сделано несущим, а не помечено декоративным — задача 6 зовёт decide_profile на КАЖДОЕ сетевое событие, а NLM стреляет несколько раз на одно физическое изменение; переприменение верной статики это дёрганье живого адаптера без выгоды. Чтобы «уже верно» было честным, сравнение обязано быть полным, поэтому добавлены и dns — Если неверно: сравнение оказалось бы недостоверным, и реализатору велено было в этом случае остановиться и сказать
Task 5: fix round 1/5 (4 addressed; commits 501f928..1c3f21e) — 396 тестов
Task 5: контроллер довёл сам: добавлено поле mask и сравнение по нему. Реализатор маску не проглядел, а обосновал её отсутствие — адрес и маску ставим одним вызовом, поэтому дрейф маски в одиночку означал бы вмешательство извне. Довод верный, но заканчивался словами «чего корректная реализация задачи 6 допускать не должна»: корректность держалась на дисциплине ещё не написанной задачи и жила в комментарии. Поле за одну строку превращает предположение в проверку
Task 5: Ruling: неполное сравнение в «уже настроено верно» опаснее лишней записи — ложное LeaveAlone при разошедшейся маске оставляет машину в чужой подсети молча и навсегда, тогда как лишний вызов netsh стоит одного дёрганья адаптера — Если неверно: одна лишняя запись при каждом событии смены сети
Task 5: тест на расхождение маски проверен мутацией: без сравнения падает, со сравнением проходит
Task 5: ОТКРЫТО ДЛЯ ЗАДАЧ 6/7 (вне объёма задачи 5): Config::validate не покрывает net_profile — не проверяется ни связность маски, ни попадание office_ip внутрь неё
Task 6: implemented — новый крейт-бинарь `crates/netsvc/` (proxypilot-netsvc),
добавлен в члены workspace; команды `install-service`/`uninstall-service` в
`crates/app/src/main.rs`. 438 тестов по всему workspace (было 397, +41 в
netsvc), клиппи и формат чисто. Сессия была прервана сбоем API ровно на
границе между библиотечными модулями (уже готовы и зелёные) и `main.rs` (ещё
заглушка) — красный прогон для библиотечных модулей подтверждён уцелевшим
файлом со скретчпада той же сессии, для остального (main.rs, тесты
install.rs, поле iface) снят заново после возобновления
Task 6: decide_profile (задача 5) не продублирован — единственная точка
встречи решения с командами netsh это netsh_cmd::commands_for_action
Task 6: отступление — ServiceProfile (формат profile.toml службы) сам не
специфицирован ни задачей 5, ни docs/design.md §7.4 буквально; определён как
{office_networks: Vec<OfficeNetwork>, net_profile: NetProfile} — оба типа
из core без изменений, потому что NetProfile один не несёт GUID офисных сетей,
без которых нельзя решить «мы в офисе»
Task 6: отступление — добавлено поле AppliedState.iface, которого не было в
исходном наброске состояния службы. Причина: adapter::gather_from_nlm находит
адаптер офисной сети только пока машина ещё в этой сети; после выхода NLM
перестаёт отдавать то подключение вовсе, а откатывать статику в DHCP всё
равно нужно на том же физическом адаптере — источник имени адаптера на этот
момент только собственная память службы
Task 6: main.rs (вход SCM, главный цикл, исполнение netsh, ICMP-пинг шлюза) не
покрыт и не может быть покрыт автотестами — задокументировано в докблоках;
единственная точка исполнения netsh (run_netsh) и единственная точка пинга
(gateway_reachable) явно названы как непроверяемые тестами
Task 6: живая read-only проверка — netsh interface ipv4 show config, разрешено
брифом. Использована для независимой сверки adapter::current_ipv4_config
(чтение GetAdaptersAddresses) с реальным выводом на домашней сети этой
машины — совпало (DHCP-флаг, адрес, маска, DNS); тест и вывод удалены сразу
после сверки, значения этой машины никуда не записаны
Task 6: НЕ выполнено ни разу — install-service, netsh set address/dnsservers
в любой форме, реальный ICMP-пинг шлюза, запуск службы. Ручные проверки
(установка, первый пуск, основной сценарий в офисе, откат при недостижимом
шлюзе, чужая статика не сбрасывается, докстанция/вторая карта, удаление)
вынесены человеку — перечислены в docs/process/win-vpn-static-ip/task-6-report.md
Task 6: complete (438 тестов, клиппи и формат чисто) — report:
docs/process/win-vpn-static-ip/task-6-report.md
Task 6: ре-ревью round 2 — NEEDS REWORK, 4 Critical + 5 Important, все из
семейства «тихий отказ»: коды возврата netsh не проверялись; провал отката
в DHCP мог стереть запись владения адаптером и по
foreign_static_address_is_never_reset (задача 5) обречь машину на
перманентный офисный адрес с мёртвым шлюзом; state писался до проверки
применения; один потерянный ICMP-пакет давал вечный цикл
применение-откат на живом адаптере; AppliedState хранил дружественное имя
адаптера вместо GUID (задача 3 уже предупреждала о переименовании); не было
таймаута netsh и SERVICE_STOP_PENDING; невыровненный Vec<u8> под
IP_ADAPTER_ADDRESSES_LH; uninstall не откатывал в DHCP; права на
%ProgramData%\ProxyPilot были номинальными; не было catch_unwind вокруг
главного цикла службы
Task 6: Ruling (округ 2) — stop НЕ обязан откатывать в DHCP (обычно это
перезагрузка, профиль применится заново), но uninstall обязан: после
DeleteService откатывать станет некому, и машина, покинувшая офис уже без
службы, держала бы статику вечно
Task 6: fix round 2 (10 findings addressed; commit поверх cbdc954) — 467
тестов по workspace (было 438, +29: часть от задачи 6 — GUID вместо
friendly name, +2 теста; остальной прирост — независимая параллельная
работа над задачей 7 в той же рабочей копии, не эта задача). Новый модуль
exec.rs — единственная точка исполнения netsh (общая с install::uninstall)
и единственная точка чтения GetAdaptersAddresses исправлена на выровненный
буфер. AppliedState.iface -> iface_guid. Латч против дребезга — только в
памяти процесса, сознательно не персистентен (перезапуск службы — обычный
человеческий способ дать ещё одну попытку)
Task 6: во время правок клиппи/fmt по всему workspace короткое время
отказывали из-за независимой параллельной работы над задачей 7 в той же
рабочей копии (crates/app/{main,tray,settings_page,websrv}.rs,
crates/winnet/{lib,routes}.rs) — не тронуто этой задачей; к финальной
проверке внешняя работа сама пришла в проходящее тесты состояние. Их
файлы, staged другой сессией, unstaged обратно (git restore --staged,
неразрушительно) перед коммитом этой задачи, чтобы не смешать два разных
task в одном коммите
Task 6: complete после ре-ревью round 2 — report дополнен:
docs/process/win-vpn-static-ip/task-6-report.md
Task 7: implemented — новый модуль crates/winnet/src/routes.rs (сбор живой
IPv4-таблицы маршрутов, GetIpForwardTable2 + GetAdaptersAddresses,
единственный поставщик Vec<AdapterRoute> для tunnel_state в проде — файл вне
списка «Files» брифа, обоснование в отчёте); раздел «Туннель» на странице
настроек (trait Tunnel + TunnelSnapshot, четыре состояния приёмки, DNS-
предупреждение до подъёма, отказ предлагать подъём над чужим туннелем, обе
привилегированные кнопки с UAC-предупреждением до клика) и его короткое
отражение в трее (статус-строка + пункт-ссылка «Туннель…», как у Bench/
Doctor). 467 тестов (было 438), клиппи и формат чисто. Report:
docs/process/win-vpn-static-ip/task-7-report.md
Task 7: получена мид-таск дизайн-коррекция от контроллера (profile.toml
службы не должен писать неповышенный процесс) — брифу задачи 7 это не
противоречило: состав задачи весь про OpenVPN-туннель, ни один пункт не
просил редактировать NetProfile/office_ip, ни один код-путь этой задачи не
трогает %ProgramData%\ProxyPilot\profile.toml
Task 7: три открытых решения, не специфицированных брифом или предыдущими
задачами: (1) our_alias = TUNNEL_PROFILE_NAME («proxypilot-office»,
предположение, не гарантия — страница говорит об этом прямо); (2) источник
для build_profile — <config_dir OpenVPN>\proxypilot-source.ovpn (ни один из
планов 1-6 не назвал, откуда его брать в проде — докблок ovpn_profile.rs
ссылался на «задачу 6», которая оказалась про статический IP службы, не про
это); (3) automate_tunnel сохраняется, но ничего не реагирует на него
подъёмом/опусканием туннеля — страница говорит и это прямо
Task 7: во время сессии в этой же рабочей копии шла отдельная, не эта задача,
переработка crates/netsvc/**, crates/core/src/netprofile.rs (судя по всему —
DACL и запись profile.toml из install-service, та самая коррекция). Коммит
задачи 7 взял только файлы, которые правила эта сессия (routes.rs,
winnet/lib.rs, app/{main,settings_page,tray,ui,websrv}.rs) — Cargo.lock и
файлы чужой правки в коммит не попали
Task 7: НЕ выполнено ни разу — openvpn-gui.exe, запись настоящего .ovpn в
реальный каталог OpenVPN, ShellExecuteW(runas)/UAC, установка/удаление
службы, netsh set. Единственные живые Windows-вызовы за сессию — чтения
(find_installation/profile_status, уже жившие в задачах 1/4, и новый
routes::gather_ipv4_routes, подтверждён смоук-тестом). Ручные проверки
(сборка реального профиля, подъём/опускание, честность our_alias на живом
адаптере, чужой туннель, установка службы с UAC) перечислены в отчёте
Task 7: complete (467 тестов, клиппи и формат чисто) — report:
docs/process/win-vpn-static-ip/task-7-report.md
Task 6: implemented (commit cbdc954) — 438 тестов; реализатор поставил DONE_WITH_CONCERNS и прямо заявил, что main.rs службы НИКОГДА не запускался и автоматическими средствами не покрывается. Это заявление и купило всё последующее ревью
Task 6: ревью — NEEDS REWORK, ЧЕТЫРЕ Critical, все одного семейства «тихий отказ»: коды возврата netsh не проверялись (под SYSTEM вывод уходит в никуда); провалившийся откат стирал запись о владении, из-за чего наша же статика на следующем цикле опознавалась как ЧУЖАЯ и оставалась навсегда — правило, защищающее чужую настройку, оборачивалось против пользователя; состояние сохранялось до подтверждения применения; одиночный пинг шлюза без задержки и повторов давал бесконечную петлю «поставили-откатили» каждые 30 секунд на живом адаптере
Task 6: ревью подтвердило чтением то, что тест не покажет: «пустой профиль ничего не трогает» защищено ДВАЖДЫ, повторный вход безопасен (один поток-потребитель), decide_profile потребляется, а не переписывается
Task 6: fix round 2/5 (10 addressed; commits cbdc954..9742d80) — 467 тестов; AppliedState перешёл на GUID адаптера вместо переименуемого имени, DACL на каталоге, catch_unwind, откат в DHCP при удалении службы
Task 6: Ruling: остановка службы НЕ возвращает DHCP, а удаление — возвращает. Остановка это обычно выключение машины, и загрузка применит профиль заново; после удаления службы вернуть адрес будет уже некому, и ноутбук уехал бы из офиса с офисной статикой навсегда — Если неверно: одно лишнее переключение в DHCP при остановке
ОШИБКА КОНТРОЛЛЕРА: задачи 6 (переработка) и 7 запущены ПАРАЛЛЕЛЬНО НА ОДНОМ рабочем дереве. Агент задачи 6 заметил чужие правки, снял их с индекса, чтобы не втянуть незавершённое в свой коммит, и сообщил. Ущерба нет, обе работы легли и уживаются (467 тестов, клиппи и формат чисты), но подставил их я. Впредь параллельные реализаторы — только на разных рабочих копиях
Task 7: implemented (commit 8dbaf81) — раздел «Туннель» на странице и в меню, новый модуль winnet::routes как единственный в проде поставщик живой таблицы маршрутов
Task 7: ПОПРАВКА ЗАМЫСЛА, отправленная в середине задачи: приложение НЕ пишет profile.toml. Спека и бриф предполагали, что его пишет неповышенное приложение в %ProgramData% — но для этого каталог обязан быть доступен пользователю на запись, и тогда любой, кто правит этот файл, диктует сетевые настройки службе от LocalSystem. Это ровно та дыра, которую отдельная копия профиля должна была закрыть, воссозданная механизмом её закрытия
Task 7: Ruling: профиль пишет ПРИВИЛЕГИРОВАННЫЙ путь (та же установка службы, единственный разрешённый UAC), поля в настройках остаются, но сохранение само по себе не применяет их и страница обязана сказать это прямо. Обещание «ядро без UAC» не нарушено: оно всегда было про прокси, мост, трей и автозапуск, а статический IP на Windows требует прав администратора по своей природе — Если неверно: смена профиля потребует лишнего повышения прав там, где хватило бы именованного канала к службе (спека 7.1 его предусматривает)
Task 7: fix round 1 (1 Critical, dispatched и устранён) — `our_alias =
TUNNEL_PROFILE_NAME` в round 0 подставлялся ещё и как псевдоним АДАПТЕРА в
`tunnel_state::{our_tunnel_up, foreign_tunnel_up}`. Ревью прочло реальные
адаптеры этой машины (`Get-NetAdapter`, только чтение) и нашло: OpenVPN
называет адаптер по драйверу («OpenVPN Wintun», «TAP-Windows Adapter V9»),
никогда — по имени профиля. Следствие: `our_tunnel_up` возвращала `false`
навсегда, а `foreign_tunnel_up` классифицировала наш же поднятый туннель как
чужой — дедлок, который задача 3 и писала `same_alias` затем, чтобы не
допустить, вернулся через значение, а не через сравнение
Task 7: fix round 1 — исправление построено на том же read-only просмотре
машины, который нашёл дефект: README.txt инсталляции OpenVPN («Logs for
connections started by the GUI are kept in %USERPROFILE%\OpenVPN\log»,
подтверждено — лог правда там, усекается заново на каждой попытке подключения:
время создания файла на годы старше времени последней записи). Новый модуль
`crates/winnet/src/tunnel_log.rs` определяет `our_tunnel_up` по этому логу,
ключуясь именем профиля — единственным идентификатором в цепочке, которым
код действительно владеет. `tunnel_state`/alias остались только для
`foreign_tunnel_up`, и только пока `our_tunnel_up == false` (проверяется
раньше в приоритете `tunnel_section`/`tunnel_text`) — регрессионные тесты
`our_confirmed_up_tunnel_wins_over_a_misclassified_foreign_reading` и
`tunnel_text_prefers_confirmed_liveness_over_a_misclassified_foreign_reading`
воспроизводят прежний ложный ввод и проверяют, что кнопка «опустить» доступна
Task 7: fix round 1 — честное «не знаю» вместо запертых кнопок: если лог
нечитаем (`liveness_error`), раздел показывает ОБЕ кнопки с предупреждением,
а не молчит, заблокировав их (та же категория дедлока, только по-другому
достигнутая). Заодно исправлены два докблока `winnet::openvpn.rs`
(`connect`, `ProfileStatus`), утверждавшие устаревшее «единственный источник
живого состояния — tunnel_state::our_tunnel_up»
Task 7: fix round 1 complete — 482 теста (было 467, +15: 10 в новом
winnet::tunnel_log), клиппи и формат чисты. Живое исследование ограничено
чтением (Get-NetAdapter, Get-Content -Tail/-TotalCount с обрезкой строк,
Get-Item, Get-ItemProperty на HKCU\Software\OpenVPN-GUI) — ничего не
подключено, не отключено, не установлено, не изменено; реальные имена
чужих log-файлов этой машины (её собственного пользователя) в отчёт и в
код не попали
Task 7: fix round 2 (1 finding, dispatched и устранён) — round 1 оставил
alias работать наполовину: пока our_tunnel_up (лог) == false,
foreign_tunnel_up всё ещё строилась по tunnel_state::foreign_tunnel_up с
тем же доказанно неверным our_alias. Остаточный случай: лог нечитаем ->
our_tunnel_up падает в false -> код мог дойти до alias-сравнения и назвать
наш же туннель чужим. Ревью потребовало убрать alias из tunnel_state
целиком, а не только из приоритета
Task 7: fix round 2 — tunnel_state::{our_tunnel_up, foreign_tunnel_up,
same_alias} удалены; новая tunnel_state::any_tunnel_carries(routes,
adapters) отвечает только «несёт ли хоть один туннельный адаптер наши
подсети», без alias вовсе. Вызывающий (WinTunnel::snapshot, main.rs)
строит our_tunnel_up/foreign_tunnel_up чистой функцией
combine_tunnel_facts из связки (лог, any_tunnel_carries) — 9 комбинаций
Result<bool>×Result<bool>, все протестированы
Task 7: fix round 2 — конъюнкция «лог Up И несёт маршруты» закрывает
hard-kill дыру, которую tunnel_log в одиночку закрыть не мог: убитый без
штатного выхода openvpn.exe продолжал бы врать «поднято» по логу, но
маршруты уходят с процессом, и any_tunnel_carries гасит ложный true.
Побочный эффект — новое честное состояние TunnelSnapshot.rising («лог уже
подтвердил, маршруты ещё не встали», короткое окно сразу после подъёма) —
не «опущен» (не приглашает нажать «Поднять» ещё раз) и не «поднят»
Task 7: fix round 2 complete (commit после 8109085) — 493 теста (было 482,
+11), клиппи и формат чисты. Живых действий не было: раунд опирался на
факты, уже установленные round 1 на живой машине, без повторного
обращения к ней. Регрессионный тест
an_unreadable_log_does_not_call_our_own_tunnel_someone_elses фиксирует
случай, который мотивировал раунд
Task 7: fix round 1/5 — КОНТРОЛЛЕР НАШЁЛ, ЧИТАЯ ЖИВУЮ МАШИНУ: our_alias был взят как имя профиля («proxypilot-office»), но Get-NetAdapter показал, что адаптеры OpenVPN называются по ДРАЙВЕРУ («OpenVPN Wintun», «TAP-Windows Adapter V9»), никогда по имени соединения. Совпадения не было бы никогда: our_tunnel_up всегда false, а собственный поднятый туннель опознавался бы как чужой — тот самый тупик задачи 3, вернувшийся через ЗНАЧЕНИЕ, а не через сравнение
Task 7: Ruling: живость берётся из журнала openvpn-gui.exe по имени профиля — единственного идентификатора, которым мы владеем сами (мы его выбираем, под ним кладём файл, с ним зовём --command). Имя адаптера нам не принадлежит и, как записала задача 3, ещё и переименуемо — Если неверно: журнал окажется ненадёжным источником, и тогда честный ответ «состояние определить нельзя» лучше догадки, которая читается как уверенность
Task 7: fix round 2/5 (commits 8109085..69d3262) — 493 теста; алиас удалён из tunnel_state ЦЕЛИКОМ (our_tunnel_up, foreign_tunnel_up(our_alias), same_alias), остался один примитив any_tunnel_carries без всякой заявки на личность
Task 7: Ruling: «наш поднят» = журнал говорит «поднят» И туннель несёт наши маршруты; «чужой» = несёт И журнал не говорит «поднят». Связка закрывает то, чего ни один источник не мог в одиночку: журнал не знает про жёстко убитый процесс (маркер остановки не допишется), маршруты не отличают наш туннель от чужого. Плюс это удалило последнего потребителя ненадёжной личности во всём продукте — Если неверно: короткое окно после подъёма читается неверно, для чего заведено отдельное честное состояние «поднимается»
Task 7: complete (commits cbdc954..69d3262) — 493 теста
ПЛАН 4 ЗАВЕРШЁН ПО КОДУ: 7 задач, 493 теста. Живые проверки статического IP и туннеля остаются за человеком — см. task-6-report.md и task-4-report.md
